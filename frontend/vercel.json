{
  "version": 2,
  "buildCommand": "rm -rf ./backend && cp -r ../backend ./backend && find ./backend -name __pycache__ -type d -prune -exec rm -rf {} + && npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "functions": {
    "api/*.py": {
      "maxDuration": 60,
      "includeFiles": "backend/**"
    }
  }
}
